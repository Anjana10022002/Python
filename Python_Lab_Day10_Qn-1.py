from abc import ABC, abstractmethod 
class User(ABC):
    def __init__(self, name, joining_year):
        self.name = name
        self.joiningYear = joining_year

    def yearCalculation(self, joining_year, ):

    
